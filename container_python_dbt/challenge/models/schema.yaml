version: 2

models:
##Nodos de unión bronze: novedad, campo segmento_time
  - name: bronze_brasil_union
    description: "La unión de todos los datasets de Brasil desduplicados y transformados"
    config:
          docs:
            node_color: '#CD7F32'
    columns:
      - name: segmento_time
        description: "Clasificación comercial de los horarios"

  - name: bronze_mexico_union
    description: "La unión de todos los datasets de Mexico, desduplicados y transformados"
    config:
          docs:
            node_color: '#CD7F32'
    columns:
      - name: segmento_time
        description: "Clasificación comercial de los horarios"


#Nodos bronze de prunning/transformación de datos
  - name: bronze_prune_brasil_01
    config:
          docs:
            node_color: '#CD7F32'
  - name: bronze_prune_brasil_31
    config:
          docs:
            node_color: '#CD7F32'
  - name: bronze_prune_mexico_30
    config:
          docs:
            node_color: '#CD7F32'
  - name: bronze_prune_mexico_31
    config:
          docs:
            node_color: '#CD7F32'
      
#Nodos silver: transformaciones de negocio
  - name: silver_aggregated_cost
    description: "Mediana del costo de publicidad por segmento horario en, base a los datos de Brasil"
    config:
          docs:
            node_color: '#C0C0C0'
  - name: silver_full_cost_brasil
    description: "Datos completos incluyendo el costo de la publicidad, sin nulos. Cuando es null, se lo reempza por la mediana de costo de publicidad correspondiente a su segmento horario. Se agrega un atributo bool que será True si el costo es verdadero y False si es estimado."
    config:
          docs:
            node_color: '#C0C0C0'
  - name: silver_full_cost_mexico
    description: "Datos completos incluyendo el costo de la publicidad, sin nulos. Cuando es null, se lo reempza por la mediana de costo de publicidad correspondiente a su segmento horario. Se agrega un atributo bool que será True si el costo es verdadero y False si es estimado. Mexico no tiene valores de costo asociados a las publicidades así que el flag será siempre False"
    config:
          docs:
            node_color: '#C0C0C0'




